import { CleanrBookingFlow } from "../../booking/CleanrBookingFlow";

/** Customer: start or continue a booking. Same booking lifecycle, customer lens. */
export function BookService() {
  return <CleanrBookingFlow />;
}
